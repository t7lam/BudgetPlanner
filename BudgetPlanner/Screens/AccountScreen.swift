import SwiftUI

struct AccountScreen: View {
    var body: some View {
        NavigationStack {
            Text("Account Screen")
                .navigationTitle("Account")
        }
    }
}

#Preview {
    AccountScreen()
} 